# Backend (Durable Object) env example
# Copy to back/.env for local development if needed.

# PartyKit dev server options (if you run the backend standalone)
# PARTYKIT_PORT=1999

# Any other env used by the back code can be documented here.
# e.g. HOSTS_KV= (not present in local partykit by default)
